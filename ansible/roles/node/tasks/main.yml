---
- name: Install Node.js and pnpm from Fedora
  become: true
  ansible.builtin.dnf5:
    name:
      - nodejs
      - nodejs-npm
      - pnpm
    state: present
  when:
    - ansible_facts.os_family == "RedHat"
    - install_node | default("0", true) == "1"
